//EXPORT BWR_Teste := 'todo';

IMPORT $;
//IMPORT Chicago_crime;
// crimes := $.File_crime_raw.File;
// OUTPUT(crimes);



// t := TABLE($.crimes,
	// {$.crimes.id});
	// dt :=(t,HASH64(id));
	// sdt := SORT(dt,id);
	// dsdt := DEDUP(sdt,id);
// COUNT(dsdt);

$.ValidInRange(10,20,30);